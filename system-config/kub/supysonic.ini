# /etc/uwsgi/supysonic.ini
[uwsgi]
    master = true
    socket = /run/uwsgi/supysonic.sock
    chmod-socket = 600
    pidfile = /run/uwsgi/supysonic.pid
    plugin = python2
    chdir = /var/supysonic
    wsgi-file = /var/supysonic/supysonic.wsgi
