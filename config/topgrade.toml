[commands]
"cargo" = "cargo iu"
"sysext" = "systemd-sysext --json=short | jaq '.[-1].extensions|@sh' -r | xargs -n1 sudo /usr/lib/systemd/systemd-sysupdate update -C"

[misc]
assume_yes = true
no_retry = false
no_self_update = false
disable = [
  "brew_cask",
  "brew_formula",
  "containers",
  "firmware",
  "helm",
  "nix",
  "pip3",
  "rustup",
  "cargo",
  "shell",
  "toolbx",
  "uv",
  "waydroid",
]
ignore_failures = []

[linux]
rpm_ostree = true

[firmware]
upgrade = false
